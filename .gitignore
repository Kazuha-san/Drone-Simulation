# --- Python engine ---------------------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/

# --- Node / Vite frontend --------------------------------------------------
node_modules/
frontend/dist/
frontend/.vite/
.eslintcache
*.tsbuildinfo

# --- Logs & debug ----------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Environment -----------------------------------------------------------
.env
.env.*
!.env.example

# --- Editors / OS ----------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
.idea/
*.swp
*~

# ---------------------------------------------------------------------------
# GENERATED, not committed:
#
#   backend/data/bhopal_map.json   <- backend/build_graph.py
#
# This app runs locally via `python3 run_app.py` (see README.md), which
# builds this file automatically on first run if it's missing. There is no
# static-hosting build step that needs it pre-committed.
#
# frontend/src/data/bhopal_basemap.json IS committed — it's the visual
# basemap the frontend needs at dev/build time, mirrored by build_graph.py.
# ---------------------------------------------------------------------------
backend/data/bhopal_map.json
